export const getAdvancedCss = (advanced, id) => {
  const {
    dimension,
    background,
    overlay,
    borderShadow,
    visibility,
    responsiveControl,
    animation,
    customCss,
  } = advanced;

  return `
  ${id}{
    
  }
  `
}